templates:
    prototype:
        baseUrl: http://localhost:3000
        # baseUrl: https://magnetic-saas.netlify.app

        routeTemplate: '/api/preview?slug=/{{@sitePath}}'
        # routeTemplate: '/{language}{{@path}}'
        $type: spaPrototype
        
mappings:
    website:
        handlePrefix: '/franklin-clockworks'
        repository: ''
