templates:
    prototype:
        baseUrl: http://localhost:3000
        # baseUrl: https://franklin-saas.netlify.app

        routeTemplate: '/api/preview?slug=/{{@path}}'
        # routeTemplate: '/{{@path}}'
        $type: spaPrototype
        
mappings:
    website:
        handlePrefix: '/franklin-clockworks'
        repository: ''
