templates:
    prototype:
        baseUrl: http://localhost:3004
        # baseUrl: https://magnetic-saas.netlify.app

        routeTemplate: '/api/preview?slug=/{{@path}}'
        # routeTemplate: '/{language}{{@path}}'
        $type: spaPrototype
        
mappings:
    website:
        handlePrefix: '/rethink'
        repository: ''
