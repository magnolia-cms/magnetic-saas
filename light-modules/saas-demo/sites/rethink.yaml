templates:
    prototype:
        # baseUrl: http://localhost:3000
        baseUrl: https://rethink-saas.netlify.app

        routeTemplate: '/api/preview?slug=/{{@path}}'
        # routeTemplate: '/{{@path}}'
        $type: spaPrototype
        
mappings:
    website:
        handlePrefix: '/rethink'
        repository: ''
