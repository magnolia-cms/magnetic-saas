templates:
    prototype:
        # baseUrl: http://localhost:3000
        baseUrl: https://magnetic-saas.netlify.app

        routeTemplate: '/api/preview?slug=/{{@path}}'
        # routeTemplate: '/{{@path}}'
        $type: spaPrototype

mappings:
    website:
        handlePrefix: '/magnetic'
        repository: ''
