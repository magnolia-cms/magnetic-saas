templates:
    prototype:
        baseUrl: http://localhost:3000
        # baseUrl: https://magnetic-saas.netlify.app

        routeTemplate: 'SITE/api/preview?slug=/{{@path}}'
        # routeTemplate: '/{language}{{@path}}'
        $type: spaPrototype
        
mappings:
    website:
        handlePrefix: '/magnetic'
        repository: ''
